name: resource

on:
  push:
    branches:
      - resource
  pull_request:
    branches:
      - resource

jobs:

  # Validate

  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Validate data
        uses: frictionlessdata/repository@main
